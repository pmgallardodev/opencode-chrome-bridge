[extend]
useDefault = true

[[allowlists]]
description = "Public RSA key that keeps the unpacked Chrome extension ID stable"
paths = ['''^extension/manifest\.json$''']
regexTarget = "match"
regexes = ['''"key"\s*:\s*"[A-Za-z0-9+/=]+"''']
